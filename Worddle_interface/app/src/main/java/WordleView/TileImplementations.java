package WordleView;

import Notifier.TileNotifier;

public class TileImplementations implements TileNotifier {
    @Override
    public void notifyTileColor() {
       /// Using the Word class, check and see which boxes should be which color



    }
}
